name: CI

on:
  push:
    branches:
      - main
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        ruby: ['3.1', '3.2', '3.3', '3.4', '4.0']
    defaults:
      run:
        working-directory: main
    steps:
      - uses: actions/checkout@v6
        with:
          path: main
          persist-credentials: false
      - name: Checkout tessel
        uses: actions/checkout@v6
        with:
          repository: rbgfx/tessel
          ref: main
          path: tessel
          persist-credentials: false
      - name: Checkout glyphic
        uses: actions/checkout@v6
        with:
          repository: rbgfx/glyphic
          ref: main
          path: glyphic
          persist-credentials: false
      - name: Resolve dependencies for this Ruby version
        run: rm Gemfile.lock
      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: ${{ matrix.ruby }}
          bundler-cache: true
          working-directory: main
      - name: Run verification
        run: bundle exec rake verify
